module ApplicationHelper
  # def previous_question(test_id)
  #   @test = test_id
  #   pre_que = @test.questions.where(["id < ?", ids]).last
  #   return pre_que
  # end

  # def next_question(test_id)
  #   @test = test_id
  #   next_que = @test.questions.where(["id > ?", ids]).first
  #   return next_que
  # end
end
