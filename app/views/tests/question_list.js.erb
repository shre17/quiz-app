if ($(document).find('#previewQuestion:visible')){
  $('#previewQuestion').replaceWith('<%= escape_javascript(render(partial: "question_list"))%>')
}
if ($(document).find('#nextQuestion:visible')){
  $('#nextQuestion').replaceWith('<%= escape_javascript(render(partial: "question_list"))%>')
}
if ($(document).find('#previousQuestion:visible')) {
  $('#previousQuestion').replaceWith('<%= escape_javascript(render(partial: "question_list"))%>')
}
